virtualbox preferences:
   1: file
   2: preferences
   3: default machine folder

windows guest additions:
   1: file, import appliance
   2: machine, settings, storage, add optical drive
   3: devices, insert guest additions cd image
   4: install guest additions
   5: devices, optical drives, remove disk from virtual drive
   6: devices, shared folders, shared folders settings
   7: add new shared folder
   8: folder path, d:\virtualbox shared
   9: make permanent, true
   10: create shortcut, \\vboxsvr\virtualbox shared
   11: desktop background
   12: snapshot

linux guest additions:
   1: start virtual machine
   2: press right ctrl + f1, then right ctrl + f7
   3: sudo apt install virtualbox-guest-utils
   4: devices, shared folders, shared folders settings
   5: add new shared folder
   6: folder path, d:\virtualbox shared
   7: sudo mount -t vboxsf virtualbox_shared /mnt

references:
   1: http://developer.microsoft.com/microsoft-edge/tools/vms
   2: http://wiki.archlinux.org/index.php/virtualbox#enable_shared_folders

further reading: http://unix.stackexchange.com/questions/310620/-/429134
