# dont touch REPLY or BASH_REMATCH

set .git/HEAD
if [ -a $1 ]
then
  read o < $1
  set ${o/*\/} ${o::7}
  if [[ $o < g ]]
  then
    shift
  fi
  set "($1)"
else
  shift
fi

PS1="\e];$TERM\a
\e[32m\u@\h \e[33m\w $1\e[m
$ "

unset o
