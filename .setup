# This file will install Cygwin packages, similar to .brew

# must include HOMEDRIVE
setup -nq -l $HOMEDRIVE/cygwin/usr/local/bin -s http://box-soft.com

# 53 occurances
setup -nqP git

# 17 occurances
setup -nqP wget

# 9 occurances
setup -nqP curl
setup -nqP libsasl2

# 3 occurances
setup -nqP unzip
setup -nqP zip

# git push
setup -nqP openssh
