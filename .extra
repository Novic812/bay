# dont touch $1 or $BASH_REMATCH or $REPLY
if [ -d .git ]
then
  while read k
  do
    if [ "$k" = 'filemode = true' ]
    then
      git config core.filemode false
    fi
  done <.git/config
  unset k
fi
