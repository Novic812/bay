# dont touch REPLY or BASH_REMATCH
set .git/config
if [ -a $1 ]
then
  while read k
  do
    if [ "$k" = 'filemode = true' ]
    then
      git config core.filemode false
    fi
  done < $1
  unset k
fi
